/**
 * Route definitions — extend when you add React Router.
 * Currently the app uses tab-based navigation inside Dashboard.
 * Swap App.jsx to use <RouterProvider> and these routes when ready.
 */
export const ROUTES = {
  HOME:      "/",
  LOGIN:     "/login",
  DASHBOARD: "/dashboard",
  SETTINGS:  "/settings",
  NOT_FOUND: "*",
};
